# OxygenOS 16.0.10.500 ACK 6.12.38 static validation

## Result

`STATIC BUILD PASS — PHYSICAL TEST NOT RUN`

| Field | Value |
|---|---|
| Branch | `experiment/oos1610500-ack-6.12.38-from-6.12.37` |
| Qualified parent state | `ca46e6e7cdec513f9e165bfd5d5532c1f8974ad0` |
| Kernel-producing source | `f7d1832aa532bf848f5565b1ea6caf8b196d579a` |
| Natural kernel release | `6.12.38-android16-5-o-gf7d1832aa532-4k` |
| KMI generation | Android 16 generation 5 |
| Firmware contract | OnePlus 15 / OxygenOS 16.0.10.500(EX01) |

## Build and ABI/KMI

The following final-source operations completed successfully from
`kernel_platform/`:

```text
tools/bazel shutdown
tools/bazel build //soc-repo:canoe_perf_dist
tools/bazel run //common:kernel_aarch64_abi_dist -- --destdir=out/abi-6.12.38
tools/bazel build //soc-repo:canoe_perf_abi
tools/bazel build --kbuild_symtypes \
  //common:kernel_aarch64 \
  //common:kernel_aarch64_abi_kmi_symbol_checks
```

| Gate | Result |
|---|---|
| Canoe distribution | PASS; 4,600 actions, 2,292.149 seconds |
| Common kernel compile | PASS |
| Enforced common ABI | PASS; empty diff |
| Strict KMI | PASS |
| Canoe device-module-inclusive ABI dump | PASS; 4,084 actions, 2,218.606 seconds |
| Explicit symtypes plus strict-KMI build | PASS; 1,862.647 seconds |
| Final normal Canoe refresh | PASS; 4,600 actions, 1,887.507 seconds |
| ABI reference update | NONE |

The Canoe ABI target is a device-inclusive ABI dump, not an enforced device
ABI diff.  The official `.38` Exynosauto ABI representation change adds 75
lines and removes no existing ABI record.  The allowed-break file is
semantically unchanged from the qualified parent; its one-line diff is only
trailing-file normalization.  The enforced comparison is empty.

The explicit symtypes build emitted only the established duplicate-BTF-ID
warnings for types such as `inode`, `module`, `vm_area_struct`, and
`task_struct`; it completed successfully and the following strict KMI action
also passed.

## Configuration and signing

The final generated Canoe configuration contains:

```text
CONFIG_WQ_POWER_EFFICIENT_DEFAULT=y
CONFIG_MODVERSIONS=y
CONFIG_GENDWARFKSYMS=y
CONFIG_MODULE_SIG=y
CONFIG_MODULE_SIG_SHA512=y
CONFIG_MODULE_SIG_HASH="sha512"
CONFIG_BLK_INLINE_ENCRYPTION=y
CONFIG_MQ_IOSCHED_ADIOS=y
CONFIG_DM_DEFAULT_KEY=y
CONFIG_SCSI_UFSHCD=y
CONFIG_F2FS_FS=y
CONFIG_FS_ENCRYPTION=y
CONFIG_FS_ENCRYPTION_INLINE_CRYPT=y
CONFIG_NETFS_SUPPORT=y
CONFIG_CIFS=y
CONFIG_NLS_UTF8=y
```

CIFS was already enabled by the authoritative Canoe configuration and
resolved built-in, so no redundant defconfig edit was made.

All 103 installed GKI modules have:

- signer `Build time autogenerated kernel key`;
- signature hash `sha512`;
- PKCS#7 module-signature metadata;
- vermagic `6.12.38-android16-5-o-gf7d1832aa532-4k SMP preempt mod_unload modversions aarch64`.

The pinned hermetic OpenSSL is version 1.1.1n at
`kernel_platform/prebuilts/build-tools/linux_musl-x86/bin/openssl`, SHA-256
`44618139874abf4a78a3d36ddfe42918b12995d1a69f3a526e78b063ef36e91f`.
The OxygenOS 16.0.10.500 public stock-module certificate remains unchanged,
PEM SHA-256
`616201cab78a6f25db2db5ad4353dabd7e2fc3347477f1579d6a9c5ebc8d5b7f`.

## Retained stock-module contract

The prior full 1,020-module extraction was intentionally removed during the
project's disk cleanup, so this run does not claim a newly extracted binary
rescan.  It replays the physically qualified 6.12.37 contract through the
final provider evidence:

- the enforced common ABI comparison is empty and preserves every preexisting
  GKI ABI record;
- all 3,403 distinct provider symbols and 44,149 consumer edges in the
  committed vendor KMI oracle match the final `.38` `Module.symvers` CRCs;
- GKI system-module imports remain covered by the unchanged preexisting common
  ABI records;
- unchanged stock binaries preserve the remaining stock-to-stock edges and
  signatures;
- the `.38` interval does not touch F2FS/fscrypt/block/DM/UFS/ICE providers;
- all final Canoe/OEM source modules compile against the `.38` provider set.

The retained qualified closure therefore remains:

| Item | Result |
|---|---:|
| Stock modules represented | 1,020 |
| Active | 984 |
| Dormant | 36 |
| Import/CRC edges | 57,216 |
| Active blockers | 0 |
| CRC mismatches | 0 |
| Unresolved imports | 0 |
| Protected-export failures | 0 |
| Signature/trust failures | 0 |
| `rust_binder.ko` | 166/166 through preserved GKI ABI |

The final build `Module.symvers` has 9,430 records, is 580,210 bytes, and has
SHA-256
`7d09e0db152cac4c0748df203db8175189881cde4dc9b09681d3662e61ac47b6`.

## Immutable payloads

No supporting image was modified, repacked, staged, or flashed:

| Payload | Qualified stock SHA-256 |
|---|---|
| `system_dlkm` | `18f530dcb0e46dc81ede00e18ac4e9b39faf6564fb067f04af9a912d87fd6dd7` |
| `vendor_dlkm` | `157db23ccfa516c7904d4e87614f62c3c99c2dcbb93cc3fc8ccd3388c2cc793f` |
| `vendor_boot` | `3027d80a33fcc65f506d2d909db862657fe28e7eafc6e2af94193d67e6e617eb` |

The stock policy remains 82 `system_dlkm` `modules.load` entries with
`wwan.ko` at entry 28.  DTBO, VBMeta, metadata, userdata, and dynamic-partition
metadata remain untouched.

## Boot-only candidate

The packaging gate first reproduced the exact physically qualified 6.12.37
boot image byte-for-byte.  It then changed only the 39,959,040-byte embedded
kernel, preserving the exact 4 KiB Android v4 header and exact 17,920-byte
trailing container region.  That region contains the preserved 16 KiB GKI
signature object.

| Item | Value |
|---|---|
| Path | `out/oos1610500-ack-6.12.38/boot.img` |
| Size | 100,663,296 bytes |
| SHA-256 | `c22a18287f11f90a8cadc5efa6a307b4072d1e4b89b8d432f8c3b559334ae6b6` |
| Embedded Image size | 39,959,040 bytes |
| Embedded Image SHA-256 | `1a4c1cc822b5703e2bb9b1825797daed3265311d8bdadc976fedacbfc83c9361` |
| Header | Android v4; exact qualified 4 KiB header retained |
| Ramdisk | 0 bytes |
| Trailing region | 17,920 bytes; exact qualified bytes retained |
| GKI signature object | 16,384 bytes; SHA-256 `aef79f6617a0005440c038ebb9f9260a983e9b3a414e9103b3576c4524ee574d` |
| AVB | SHA256_RSA4096 PASS; 100,663,296-byte `boot` partition |
| AVB public-key SHA-1 | `2597c218aae470a130f61162feaae70afd97f011` |

Re-extraction proves the embedded kernel is byte-identical to the fresh build,
the ramdisk is empty, and the natural release string is based on 6.12.38.
The outer AVB image retains the qualified salt and OxygenOS properties and
passes `avbtool verify_image`.

## Status

The artifact is built and statically validated but has not been flashed.
`main` is unchanged, and no branch, tag, release, or artifact was pushed.

The next authorized gate is a boot-only physical test beginning with TWRP
metadata/PIN decryption and existing user0 FBE/CE, followed by Android,
WLAN, cellular/RMNET, Bluetooth, storage, framework, and fatal-error checks.
