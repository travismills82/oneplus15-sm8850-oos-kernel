# OOS 16.0.10.500 ACK 6.12.26 to 6.12.27 audit

## Scope

This controlled point-release experiment starts from the physically qualified
OxygenOS 16.0.10.500 checkpoint:

- parent: `8c18383461dfef341c9395b9fbc95a795a698a21`;
- branch: `experiment/oos1610500-ack-6.12.27`;
- parent kernel: Android 16 generation 5, Linux 6.12.26;
- firmware contract: exact stock `system_dlkm`, `vendor_dlkm`, and
  `vendor_boot` for CPH2747 OxygenOS 16.0.10.500(EX01).

No DLKM, vendor boot, DTBO, VBMeta, userdata, metadata, or module-load policy
change belongs to this experiment.

## Official boundary

The exact official Android Common generation-5 boundary is:

| State | Official commit | Proof |
|---|---|---|
| complete 6.12.26 | `c72e4daa7ab2814e402f2b67dd1681ea4db446af` | `Linux 6.12.26`; `SUBLEVEL = 26` |
| first 6.12.27 interval commit | `92c6f8c577a0ca52d779d3a4d42465bd81f854a1` | direct child of the 6.12.26 release commit |
| complete 6.12.27 | `b801eaa96a5a2f2a8fe0b5982ffe4a1fba3c1e93` | `Linux 6.12.27`; `SUBLEVEL = 27` |

The complete interval contains exactly two ordered commits and touches only:

```text
kernel/bpf/preload/bpf_preload_kern.c
Makefile
```

The first commit corrects the stable-branch `MODULE_IMPORT_NS()` grammar for
`BPF_INTERNAL`. The second changes the source sublevel from 26 to 27. There is
no block, storage, FBE, crypto, KMI type, exported-symbol, or device-tree source
change in this interval.

## Commit-by-commit ancestry

Both official commits were replayed individually in their original order with
their original authors, author dates, subjects, commit messages, and
Signed-off-by trailers:

| Official commit | Repository commit | Subject |
|---|---|---|
| `92c6f8c577a0ca52d779d3a4d42465bd81f854a1` | `793d3faa9246539c008a89570b0e0357e6a699f3` | `bpf: Fix BPF_INTERNAL namespace import` |
| `b801eaa96a5a2f2a8fe0b5982ffe4a1fba3c1e93` | `fa3c82a7f526c9f81483599ce80034154cbed316` | `Linux 6.12.27` |

No upstream commit was squashed, folded into an aggregate snapshot, skipped,
or rewritten as a downstream-authored source commit. Both patches applied
without a conflict. The ordered machine-readable record is
`oos1610500-ack-6.12.27-commits.tsv`.

## Pending gates

The source replay alone does not qualify the point release. Before physical
testing it must pass the Canoe distribution build, generated configuration
comparison, enforced common ABI comparison, KMI checks, Canoe ABI dump, exact
1,020-module stock binary contract, boot-container validation, and immutable
stock payload checks. Physical qualification remains a separate gate.
