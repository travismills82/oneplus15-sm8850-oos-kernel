# OxygenOS 16.0.10.500 ACK 6.12.39 static validation

## Result

`STATIC BUILD PASS — PHYSICAL TEST NOT RUN`

| Field | Value |
|---|---|
| Branch | `experiment/oos1610500-ack-6.12.39-from-6.12.38` |
| Qualified parent repository state | `e73f86fd0ae35418eb24996540fa81173fb5505b` |
| Kernel-producing source | `818eda14c009acde3ed1ebeb8c700eb72e62610b` |
| Natural kernel release | `6.12.39-android16-5-o-g818eda14c009-4k` |
| KMI generation | Android 16 generation 5 |
| Firmware contract | OnePlus 15 / OxygenOS 16.0.10.500(EX01) |

## ACK provenance

Android Common does not publish an `android16-6.12.39_r00` tag. The completed
boundary is the official `android16-6.12-lts` merge
`8eba32b6565d4041bd6fd4530b1b62fe7a45f6f1`, whose Linux-stable parent is
`cdf264c0a5906063a0e2b750d420d77cb992446d` (`Linux 6.12.39`).

The interval contains 167 non-merge Linux-stable commits. All were audited in
order: 163 became new individual upstream-derived commits, three were already
present in the qualified ancestry, and one older eventpoll implementation was
superseded by a later complete security series already in the parent. Android
Common's immediate `.39` DRM KABI repair
`4a5ce5aeeca24b349f541116f022e80532bcad76` is preserved separately as local
commit `91380ffbe5a1`. In total, 164 new official commits are individually
visible, with original authorship, author dates, subjects, bodies, order, and
provenance retained. No 6.12.40 commit is present.

## Build and ABI/KMI

The following final-source operations completed successfully from
`kernel_platform/`:

```text
tools/bazel build //soc-repo:canoe_perf_dist
tools/bazel run //common:kernel_aarch64_abi_dist -- --destdir=out/abi-6.12.39-final2-20260914
tools/bazel build --kbuild_symtypes \
  //common:kernel_aarch64 \
  //common:kernel_aarch64_abi_kmi_symbol_checks \
  //soc-repo:canoe_perf_abi
tools/bazel build //soc-repo:canoe_perf_dist
```

| Gate | Result |
|---|---|
| Initial complete Canoe distribution | PASS; 997 actions, 2,794.012 seconds |
| Enforced common ABI | PASS; all five reports empty |
| Strict KMI | PASS |
| Symtypes | PASS |
| Canoe device-module-inclusive ABI dump | PASS |
| Combined symtypes/KMI/Canoe ABI run | PASS; 4,695-action graph, 695 executed actions and 4,018 cache hits, 4,051.972 seconds |
| Final normal Canoe refresh | PASS; 4,600 actions, 1,774.770 seconds |
| ABI reference update | NONE |

The Canoe ABI target remains a device-inclusive ABI dump rather than an
enforced device ABI diff. The Linux-stable DRM framebuffer change initially
changed 191 generation-5 CRCs even though its member occupied existing
padding. The candidate uses the exact official Android `.39` revert, which
restores the frozen ABI without CRC manipulation, KABI masking, or a reference
update. The enforced final common ABI comparison is empty.

The builds emitted the established duplicate-BTF-ID notices, existing Canoe
device-tree warnings, and existing depmod cross-package staging warnings for
symbols supplied outside the isolated staging set. None was a compilation,
KMI, ABI, or final provider-contract failure. `git diff --check` reports the
exact upstream Intel SOF file's trailing blank line; its content is retained
unchanged from the official commit.

## Configuration and signing

The final generated Canoe configuration has SHA-256
`42ff31797a5dd965f549e2ff8719105791837abef8d99cb82c511cf2160d1e08` and
retains the qualified feature and storage contract:

```text
CONFIG_WQ_POWER_EFFICIENT_DEFAULT=y
CONFIG_MODVERSIONS=y
CONFIG_GENDWARFKSYMS=y
CONFIG_MODULE_SIG=y
CONFIG_MODULE_SIG_SHA512=y
CONFIG_MODULE_SIG_HASH="sha512"
CONFIG_BLK_INLINE_ENCRYPTION=y
CONFIG_BLK_INLINE_ENCRYPTION_FALLBACK=y
CONFIG_MQ_IOSCHED_DEADLINE=y
CONFIG_MQ_IOSCHED_KYBER=y
CONFIG_MQ_IOSCHED_ADIOS=y
CONFIG_IOSCHED_BFQ=y
CONFIG_BLK_DEV_DM=y
CONFIG_DM_CRYPT=y
CONFIG_DM_DEFAULT_KEY=y
CONFIG_SCSI_UFSHCD=y
CONFIG_SCSI_UFS_CRYPTO=y
CONFIG_F2FS_FS=y
CONFIG_F2FS_FS_SECURITY=y
CONFIG_F2FS_FS_COMPRESSION=y
CONFIG_FS_ENCRYPTION=y
CONFIG_FS_ENCRYPTION_INLINE_CRYPT=y
CONFIG_NETFS_SUPPORT=y
CONFIG_CIFS=y
CONFIG_NLS_UTF8=y
```

No ADIOS-default symbol is selected; ADIOS remains built in and selectable,
not the default scheduler. The `.39` interval did not change the authoritative
arm64 GKI defconfig.

All 103 final normal-build GKI modules have:

- signer `Build time autogenerated kernel key`;
- signature hash `sha512`;
- a valid appended PKCS#7 module-signature marker;
- vermagic `6.12.39-android16-5-o-g818eda14c009-4k SMP preempt mod_unload modversions aarch64`.

The pinned hermetic OpenSSL is version 1.1.1n at
`kernel_platform/prebuilts/build-tools/linux_musl-x86/bin/openssl`, SHA-256
`44618139874abf4a78a3d36ddfe42918b12995d1a69f3a526e78b063ef36e91f`.
The OxygenOS 16.0.10.500 public stock-module certificate remains unchanged,
PEM SHA-256
`616201cab78a6f25db2db5ad4353dabd7e2fc3347477f1579d6a9c5ebc8d5b7f`.

## Retained stock-module contract

The prior full 1,020-module extraction remains intentionally absent after the
project disk cleanup, so this run does not claim a new binary extraction. It
replays the physically qualified 6.12.38 contract against the final provider
evidence:

- the enforced common ABI comparison is empty;
- all 3,403 provider symbols and 44,149 vendor/vendor-boot consumer edges in
  the committed oracle match the final `Module.symvers`;
- the complete `.39` source audit shows no F2FS, fscrypt, blk-crypto,
  dm-default-key, dm-crypt, SCSI/UFS crypto, Qualcomm ICE, config, signing,
  trusted-key, ABI-definition, or KMI-list change;
- all final Canoe/OEM modules compile against the resulting provider set;
- Rust Binder remains 166/166 through the unchanged frozen GKI ABI.

| Item | Result |
|---|---:|
| Stock modules represented | 1,020 |
| Active | 984 |
| Dormant | 36 |
| Import/CRC edges in retained qualified closure | 57,216 |
| Active blockers | 0 |
| CRC mismatches | 0 |
| Unresolved imports | 0 |
| Protected-export failures | 0 |
| Signature/trust failures | 0 |
| `rust_binder.ko` | 166/166 through preserved GKI ABI |

The final normal-build `Module.symvers` has 9,430 records, is 580,210 bytes,
and has SHA-256
`7d09e0db152cac4c0748df203db8175189881cde4dc9b09681d3662e61ac47b6`.

## Immutable payloads

No supporting image was modified, repacked, staged for physical use, or
flashed:

| Payload | Qualified stock SHA-256 |
|---|---|
| `system_dlkm` | `18f530dcb0e46dc81ede00e18ac4e9b39faf6564fb067f04af9a912d87fd6dd7` |
| `vendor_dlkm` | `157db23ccfa516c7904d4e87614f62c3c99c2dcbb93cc3fc8ccd3388c2cc793f` |
| `vendor_boot` | `3027d80a33fcc65f506d2d909db862657fe28e7eafc6e2af94193d67e6e617eb` |

The stock policy remains 82 `system_dlkm` `modules.load` entries with
`wwan.ko` at entry 28. DTBO, VBMeta, metadata, userdata, and dynamic-partition
metadata remain untouched.

## Boot-only candidate

The candidate changes only the 39,959,040-byte production `Image` inside the
exact physically qualified 6.12.38 boot container. It preserves the exact
4,096-byte Android v4 header and exact 17,920-byte trailing region, including
the 16,384-byte GKI signature object, and regenerates the outer AVB hash footer
using the qualified salt, metadata, key, partition name, and capacity.

| Item | Value |
|---|---|
| Path | `out/oos1610500-ack-6.12.39/boot.img` |
| Size | 100,663,296 bytes |
| SHA-256 | `25cabaa8e7fcfefc11c524080e2a7213f94a5290d31af8c1bb2386365e61a826` |
| Embedded Image size | 39,959,040 bytes |
| Embedded Image SHA-256 | `4286999eb46dde5db3416acca5bfcbdfd7e931cb45f2d026e0e7d911249ed04e` |
| Header | Android v4; exact qualified 4 KiB header retained |
| Ramdisk | 0 bytes |
| Trailing region | 17,920 bytes; exact qualified bytes retained |
| GKI signature object | 16,384 bytes; SHA-256 `aef79f6617a0005440c038ebb9f9260a983e9b3a414e9103b3576c4524ee574d` |
| AVB | SHA256_RSA4096 PASS; 100,663,296-byte `boot` partition |
| AVB public-key SHA-1 | `2597c218aae470a130f61162feaae70afd97f011` |

Re-extraction proves the embedded kernel is byte-identical to the fresh normal
build, the ramdisk is empty, and the natural release string is based on
6.12.39. `avbtool verify_image` passes.

## Status

The artifact is built and statically validated but has not been flashed. The
remote `main` branch remains the physically proven 6.12.38 state. The 6.12.39
experiment branch, validation commit, boot artifact, tag, and release have not
been pushed or published.

The next gate is a boot-only physical test beginning with TWRP metadata/PIN
decryption and existing user0 FBE/CE, followed by Android, WLAN,
cellular/RMNET, Bluetooth, storage, framework, and fatal-error checks.
