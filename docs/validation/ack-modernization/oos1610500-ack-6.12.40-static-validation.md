# OxygenOS 16.0.10.500 ACK 6.12.40 static validation

## Identity and scope

| Item | Value |
|---|---|
| Development branch | `experiment/oos1610500-ack-6.12.40-from-6.12.39` |
| Physically accepted parent | `72c74ef9a1023fcfc39f50ce3d4ad0244503d553` |
| Kernel-producing source | `493c03afce9748a457503e0474d33da789ed3ad9` |
| Official tag | `android16-6.12.40_r00` |
| Official tag object | `36808d2f48b7e89f7ca97bad8ce13230cff9bf2e` |
| Official peeled commit | `c026dd5eaf39a29cfac9ed1405778d420ec79939` |
| Kernel release | `6.12.40-android16-5-o-g493c03afce97-4k` |
| KMI generation | `5` |
| Firmware contract | OxygenOS 16.0.10.500(EX01) stock supporting partitions |

The official `.39` to `.40` graph contains 231 non-merge records.  The local
history preserves 213 newly applicable records as individual commits with
their upstream authorship, subjects, bodies, order, and provenance.  Eighteen
records were already present, empty, target-inapplicable, or deliberately
excluded because an official vendor-specific cleanup would remove an existing
qualified generation-5 ABI record.  The complete mapping is in
`oos1610500-ack-6.12.40-commits.tsv`; no official ACK interval was squashed.

The manifest pin is a separate commit.  The only downstream runtime/KMI
reconciliation is `493c03afce9748a457503e0474d33da789ed3ad9`, which retains
six qualified F2FS/UFS tracepoint exports after the official interval removed
them.  It neither changes ABI/KMI references nor relaxes enforcement.

## Build and ABI/KMI validation

The final production build was generated with:

```sh
cd kernel_platform
tools/bazel build //soc-repo:canoe_perf_dist
```

Result: **PASS**.  The final refresh executed 25 actions with 4,589 cache hits
after the preceding complete build; the preceding complete Canoe distribution
executed 994 actions.  Warnings were limited to the established device-tree,
deprecated-build-rule, and depmod staging warnings.

The exact enforced common ABI command was:

```sh
tools/bazel run //common:kernel_aarch64_abi_dist -- \
  --destdir=out/abi-6.12.40-20260915-085303
```

Result: **PASS**, empty ABI difference.  No ABI reference was updated.

The exact symtypes, strict KMI, and Canoe device-module-inclusive ABI dump
command was:

```sh
tools/bazel build --kbuild_symtypes \
  //common:kernel_aarch64 \
  //common:kernel_aarch64_abi_kmi_symbol_checks \
  //soc-repo:canoe_perf_abi
```

Result: **PASS**.  The Canoe target is an ABI dump, not an enforced device ABI
diff.  The build emitted only the previously established duplicate-BTF-ID
warnings.  `Module.symvers` contains 9,490 records, is 584,025 bytes, and has
SHA-256 `5352a95a60639ba70544f52612996f674c78023cfbe1190d65d126fc67ffa354`.

## Effective Canoe configuration

The authoritative generated Canoe configuration is
`bazel-bin/soc-repo/canoe_perf_config/out_dir/.config`, not the standalone GKI
intermediate.  It is 256,446 bytes with SHA-256
`b043fbd4daca4de0f7556620127dc2aa68bfafa265282f5c109e06eb0a5cc3d3`.
The final values include:

```text
CONFIG_WQ_POWER_EFFICIENT_DEFAULT=y
CONFIG_MODVERSIONS=y
CONFIG_GENDWARFKSYMS=y
CONFIG_MODULE_SIG=y
CONFIG_MODULE_SIG_SHA512=y
CONFIG_MODULE_SIG_HASH="sha512"
CONFIG_BLK_INLINE_ENCRYPTION=y
CONFIG_BLK_INLINE_ENCRYPTION_FALLBACK=y
CONFIG_MQ_IOSCHED_DEADLINE=y
CONFIG_MQ_IOSCHED_KYBER=y
CONFIG_MQ_IOSCHED_ADIOS=y
CONFIG_IOSCHED_BFQ=y
CONFIG_BLK_DEV_DM=y
CONFIG_DM_CRYPT=y
CONFIG_DM_DEFAULT_KEY=y
CONFIG_SCSI_UFSHCD=y
CONFIG_SCSI_UFS_CRYPTO=y
CONFIG_F2FS_FS=y
CONFIG_F2FS_FS_SECURITY=y
CONFIG_F2FS_FS_COMPRESSION=y
CONFIG_FS_ENCRYPTION=y
CONFIG_FS_ENCRYPTION_INLINE_CRYPT=y
CONFIG_NETFS_SUPPORT=y
CONFIG_CIFS=y
CONFIG_NLS_UTF8=y
```

`CONFIG_MQ_IOSCHED_DEFAULT_ADIOS` is not set.  ADIOS remains selectable and is
not made the default scheduler.

## Signing and retained module contract

The production module set contains 103 stripped GKI modules.  All 103 have an
appended module signature, a PKCS#7 signature, the expected
`Build time autogenerated kernel key` signer, SHA-512, and exact 6.12.40
vermagic.  The hermetic signer is OpenSSL 1.1.1n at
`prebuilts/build-tools/linux_musl-x86/bin/openssl`, SHA-256
`44618139874abf4a78a3d36ddfe42918b12995d1a69f3a526e78b063ef36e91f`.
The stock public module certificate remains unchanged with SHA-256
`616201cab78a6f25db2db5ad4353dabd7e2fc3347477f1579d6a9c5ebc8d5b7f`.
Stock `rust_binder.ko` retains all 166 versioned imports.

The retained qualified provider oracle contains 3,403 provider symbols and
44,149 vendor/vendor_boot consumer edges.  Replaying it against the final
6.12.40 `Module.symvers` found zero missing providers and zero CRC mismatches.
The last extracted full current-firmware closure remains 1,020 modules, 57,216
import edges, 984 active modules, and 36 dormant modules.  A new extraction of
all proprietary images was not performed for this point build; this report
does not mislabel the retained-oracle replay as a new complete binary scan.

The stock payload contract remains byte-identical:

| Payload | SHA-256 |
|---|---|
| `system_dlkm` | `18f530dcb0e46dc81ede00e18ac4e9b39faf6564fb067f04af9a912d87fd6dd7` |
| `vendor_dlkm` | `157db23ccfa516c7904d4e87614f62c3c99c2dcbb93cc3fc8ccd3388c2cc793f` |
| `vendor_boot` | `3027d80a33fcc65f506d2d909db862657fe28e7eafc6e2af94193d67e6e617eb` |

The stock load policy remains 82 `modules.load` entries with `wwan.ko` at
entry 28.  No DLKM, vendor_boot, DTBO, VBMeta, userdata, or metadata image was
modified or packaged.

## Boot-only artifact

The production Image is 40,024,576 bytes with SHA-256
`fbeeae648b5dd551da330bec6c6629380b4b63a741b36cc654b502be06f52af2`.
Its embedded release string is the natural build identity:

```text
6.12.40-android16-5-o-g493c03afce97-4k
```

The boot image was packaged using the physically established OxygenOS
16.0.10.500 Android-v4 contract:

1. preserve the qualified 4,096-byte Android-v4 header, changing only the
   little-endian kernel-size field from 39,959,040 to 40,024,576 bytes;
2. replace the embedded Image with the exact production Image above;
3. preserve the complete qualified 17,920-byte trailing region, including the
   exact 16,384-byte GKI signature object;
4. regenerate only the outer boot AVB footer for the larger truthful original
   image size, using the qualified `.500` salt, properties, partition name,
   algorithm, and key identity.

Structural verification passed:

| Check | Result |
|---|---|
| Android boot magic and header version 4 | PASS |
| Header identical to qualified `.39` except kernel-size field | PASS |
| Embedded Image exact SHA-256 match | PASS |
| Complete 17,920-byte trailing region exact match | PASS |
| 16,384-byte GKI signature region exact match | PASS |
| AVB SHA256_RSA4096 footer verification | PASS |
| Partition name `boot` and 96 MiB capacity | PASS |
| `.500` fingerprint/security patch/salt preserved | PASS |

Artifact:

| Item | Value |
|---|---|
| Path | `out/oos1610500-ack-6.12.40/boot.img` |
| Size | 100,663,296 bytes |
| SHA-256 | `f0876713541f72684bd97a90483beb5043ee4656fceb42ca8627b79690feeb35` |
| AVB original image size | 40,046,592 bytes |
| Embedded Image SHA-256 | `fbeeae648b5dd551da330bec6c6629380b4b63a741b36cc654b502be06f52af2` |
| Preserved trailing-region SHA-256 | `a1f7158a3968d8e0399802c4f6ad9122ba3afc6c37a8c0cb2a0018223f9cc89c` |
| Preserved GKI-signature-region SHA-256 | `aef79f6617a0005440c038ebb9f9260a983e9b3a414e9103b3576c4524ee574d` |
| AVB public-key SHA-1 | `2597c218aae470a130f61162feaae70afd97f011` |

## Qualification status

Static build, configuration, enforced common ABI, strict KMI, Canoe ABI dump,
signing, retained-provider, and boot-container gates pass.  This exact image
has **not** been flashed or physically tested.  The phone remains on the
physically accepted 6.12.39 kernel.  Therefore 6.12.40 is a static candidate,
not a qualified branch, tag, release, or `main` state.
