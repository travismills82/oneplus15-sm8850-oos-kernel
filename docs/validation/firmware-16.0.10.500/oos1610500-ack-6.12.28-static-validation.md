# OOS 16.0.10.500 ACK 6.12.28 OOS compatibility validation

## Result

**PASS — READY FOR A CONTROLLED BOOT-ONLY USER0 PHYSICAL TEST.**

The Android/OOS-compatible 6.12.28 candidate passes the complete static gate.
It has not been flashed and is not physically qualified. No 6.12.29 source is
present.

| Field | Value |
|---|---|
| Branch | `experiment/oos1610500-ack-6.12.28-oos-compat` |
| Plain static-bad 6.12.28 | `8227c049152e` |
| CPU-frequency compatibility | `25801042c501` |
| XSK compatibility | `07a0a465cbb98217328f53bf8cec9bd9d7f7bd8f` |
| Built source HEAD | `07a0a465cbb98217328f53bf8cec9bd9d7f7bd8f` |
| Natural kernel release | `6.12.28-android16-5-o-g07a0a465cbb9-4k` |
| KMI generation | 5 |
| Firmware contract | CPH2747 OxygenOS 16.0.10.500(EX01) |

The exact 6.12.28 replay remains 166 individually reviewable official
commits. The final upstream replay is `997731144aae` (`Linux 6.12.28`), with
the separate exact-boundary cpufreq completion at `8227c049152e`. The two
compatibility changes remain separate downstream commits.

## Build and ABI/KMI gates

The following commands completed successfully from `kernel_platform/` using
the final committed source:

```text
tools/bazel build //soc-repo:canoe_perf_dist
tools/bazel build --kbuild_symtypes //common:kernel_aarch64
tools/bazel run //common:kernel_aarch64_abi_dist -- \
  --destdir=out/oos1610500-ack-6.12.28-oos-compat-abi-dist
tools/bazel build \
  //common:kernel_aarch64_abi \
  //common:kernel_aarch64_abi_kmi_symbol_checks \
  //common:kernel_aarch64_abi_diff
tools/bazel build //soc-repo:canoe_perf_abi
tools/bazel run //soc-repo:canoe_perf_dist
```

| Gate | Result |
|---|---|
| Canoe distribution | PASS |
| Symtypes | PASS |
| Enforced common ABI | PASS; all five `abi_stgdiff` reports are empty |
| Strict KMI symbol list | PASS |
| Common ABI targets | PASS |
| Canoe device-inclusive ABI dump | PASS |

The Canoe ABI target is recorded as a device-inclusive dump, not mislabeled
as an enforced device ABI diff. The complete retained-stock binary audit below
is the device-module contract gate.

Warnings remain in known downstream classes: DTS schema style, duplicate BTF
IDs, SCM version strings, deprecated Kleaf APIs, Oplus Python syntax, and
existing `depmod` warnings for downstream modules. No new fatal or
compatibility warning class was identified.

## Configuration, FBE, and signing

The generated 6.12.27 and compatible 6.12.28 configurations have no effective
semantic difference. Besides the generated version comment, the only textual
change is removal of the redundant
`# CONFIG_SND_SOC_CS_AMP_LIB_TEST is not set` line.

Required configuration remains enabled:

```text
CONFIG_MODVERSIONS=y
CONFIG_GENDWARFKSYMS=y
CONFIG_MODULE_SIG=y
CONFIG_MODULE_SIG_SHA512=y
CONFIG_TRIM_UNUSED_KSYMS=y
CONFIG_BLK_INLINE_ENCRYPTION=y
CONFIG_DM_DEFAULT_KEY=y
CONFIG_SCSI_UFSHCD=y
CONFIG_F2FS_FS=y
CONFIG_FS_ENCRYPTION=y
CONFIG_FS_ENCRYPTION_INLINE_CRYPT=y
```

The 6.12.28 FBE/storage interval contains the reviewed dm-integrity, dm argv,
UFS tracing, blk-mq fallback-map, and dm bounds fixes. It contains no fscrypt,
F2FS, inline-encryption, or dm-default-key policy change. This is a static FBE
pass; existing encrypted user0 remains the first physical oracle.

The pinned signer is
`kernel_platform/prebuilts/build-tools/linux_musl-x86/bin/openssl`, OpenSSL
1.1.1n, SHA-256
`44618139874abf4a78a3d36ddfe42918b12995d1a69f3a526e78b063ef36e91f`.
All 103 source-built GKI modules report the expected autogenerated build key,
SHA-512, and PKCS#7. No host OpenSSL fallback was used.

## Complete stock module contract

| Gate | Result |
|---|---:|
| Modules checked | 1,020 |
| Active compatible | 984 |
| Dormant | 36 |
| Import/CRC edges | 57,216 |
| Active blockers | 0 |
| Unresolved imports | 0 |
| CRC mismatches | 0 |
| Protected-export failures | 0 |
| Signature/trust failures | 0 |
| Structural-provider failures | 0 |
| `rust_binder.ko` | 166/166 imports compatible |
| Stock `system_dlkm` `modules.load` | 82 entries |
| `wwan.ko` position | 28 |

Intermediate measurement proves cpufreq restored the GKI ABI and XSK removed
the complete 12-module/16-edge runtime blocker set. The final audit was rerun
against the committed `07a0a465cbb9` `Module.symvers`, not an intermediate
build.

## Immutable stock payload contract

The stock images were re-hashed directly from the OxygenOS 16.0.10.500 image
set. None was written, modified, repacked, or staged as a candidate:

| Payload | Size | SHA-256 |
|---|---:|---|
| stock `system_dlkm.img` (EROFS) | 14,131,200 | `18f530dcb0e46dc81ede00e18ac4e9b39faf6564fb067f04af9a912d87fd6dd7` |
| stock `vendor_dlkm.img` | 144,105,472 | `157db23ccfa516c7904d4e87614f62c3c99c2dcbb93cc3fc8ccd3388c2cc793f` |
| stock `vendor_boot.img` | 100,663,296 | `3027d80a33fcc65f506d2d909db862657fe28e7eafc6e2af94193d67e6e617eb` |

The stock `.500` public module certificate remains unchanged, with SHA-256
`616201cab78a6f25db2db5ad4353dabd7e2fc3347477f1579d6a9c5ebc8d5b7f`,
serial `6E2DD27B61C8671D2C2AFB38E907FE98DC8F0230`, and SHA-256 fingerprint
`F2:8D:BC:C6:00:85:B2:1A:3C:FF:13:42:48:28:97:FA:64:0B:46:88:47:47:31:47:83:4F:26:C4:FE:B2:DF:43`.

## Boot-only artifact

Only the physical-test `boot.img` was retained. The container uses Android
boot header v4, an empty ramdisk, the exact fresh `Image`, the qualified
16-KiB `.500` GKI signature object, and the qualified firmware AVB properties
and salt.

| Artifact | Size | SHA-256 |
|---|---:|---|
| `Image` | 39,954,944 | `f7faadab51a1e97ab4fa80c2551bb60723f6db1046599690b6ebaa3ea1ea0e6c` |
| GKI signature object | 16,384 | `aef79f6617a0005440c038ebb9f9260a983e9b3a414e9103b3576c4524ee574d` |
| `boot.img` | 100,663,296 | `fd21e481cb11aec2442b3e854c30a393eb1cb51cdebe1542fc2b700c1d8a684b` |

Re-extraction reports a 39,954,944-byte kernel, zero-byte ramdisk, and header
version 4. The extracted kernel is byte-identical to the built `Image`. The
un-padded AVB image length is 39,976,960 bytes, leaving 60,686,336 bytes in the
configured boot partition. The appended GKI signature bytes match the
qualified object. AVB footer and payload verification pass with public-key
SHA-1 `2597c218aae470a130f61162feaae70afd97f011`.

Candidate path:

```text
out/oos1610500-ack-6.12.28-oos-compat/boot.img
```

## Physical gate

No flash was performed. The next operation must be a controlled boot-only
test from the exact qualified 6.12.27 state: verify backup and rollback,
write only the active test-slot boot partition, verify the complete read-back,
and attempt one boot. PASS first requires normal Android, ADB,
`sys.boot_completed=1`, existing user0 `RUNNING_UNLOCKED`, FBE/CE access, and
no `init_user0_failed`. Full radio, storage, framework, error-scan, suspend,
and two-clean-boot qualification follows only after that oracle passes.
