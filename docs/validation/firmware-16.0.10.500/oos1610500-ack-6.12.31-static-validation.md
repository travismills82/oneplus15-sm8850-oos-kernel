# OOS 16.0.10.500 ACK 6.12.31 static validation

## Result

**PASS — READY FOR A CONTROLLED BOOT-ONLY USER0 PHYSICAL TEST.**

The candidate passes the complete static compatibility gate. It has not been
flashed, pushed, merged, tagged, released, or physically qualified.

| Field | Value |
|---|---|
| Branch | `experiment/oos1610500-ack-6.12.31-from-6.12.30` |
| Physically booted parent | `ba43d41251812df0f07d13189174e3174e2b46d8` |
| Complete local 6.12.31 replay | `455f5c740737bc6b012d63e67c7086e276865955` |
| Built source | `e42834ea2f518a636677f1c7313874cd6fdbad16` |
| Natural kernel release | `6.12.31-android16-5-o-ge42834ea2f51-4k` |
| KMI generation | 5 |
| Firmware contract | CPH2747 OxygenOS 16.0.10.500(EX01) |

## Build and ABI/KMI gates

The following commands completed successfully from `kernel_platform/`:

```text
tools/bazel build --kbuild_symtypes //common:kernel_aarch64

tools/bazel build \
  //common:kernel_aarch64_abi \
  //common:kernel_aarch64_abi_kmi_symbol_checks \
  //common:kernel_aarch64_abi_diff

tools/bazel run //common:kernel_aarch64_abi_dist -- \
  --destdir=out/oos1610500-ack-6.12.31-abi-dist-20260904-174328

tools/bazel build //soc-repo:canoe_perf_abi
tools/bazel build //soc-repo:canoe_perf_dist
tools/bazel run //soc-repo:canoe_perf_dist
```

| Gate | Result |
|---|---|
| Final Canoe distribution build | PASS; 3,989 actions, 1,006.699 seconds |
| Explicit common symtypes | PASS; 1,370.080 seconds |
| Common ABI targets | PASS; 1,393.617 seconds |
| Strict KMI symbol list | PASS |
| Enforced common ABI distribution | PASS; empty short report |
| Direct final-vs-qualified symtypes comparison | PASS; `stgdiff` return code 0 |
| Canoe device-inclusive ABI dump | PASS; 4,084 actions, 2,362.142 seconds |
| Dist execution and copy | PASS |

The Canoe ABI target is a device-inclusive dump, not an enforced device ABI
diff. The retained-stock binary audit below is the authoritative device-module
gate.

The final `kernel_aarch64_Module.symvers` contains 9,356 symbols and is
byte-for-byte identical to the qualified 6.12.30 state, SHA-256
`6889e56eb427705a9002c7b030b2af7b9a6153c874efb1cda5ddaf56be60aa27`.
There are zero added, removed, or CRC-changed exports.

## Configuration, FBE, and signing

After excluding only the generated kernel-version comment, the final 6.12.30
and 6.12.31 Canoe `.config` files are byte-identical. The effective symbol
delta is zero.

```text
CONFIG_WQ_POWER_EFFICIENT_DEFAULT=y
CONFIG_MQ_IOSCHED_ADIOS=y
CONFIG_MODVERSIONS=y
CONFIG_GENDWARFKSYMS=y
CONFIG_MODULE_SIG=y
CONFIG_MODULE_SIG_SHA512=y
CONFIG_MODULE_SIG_HASH="sha512"
CONFIG_BLK_INLINE_ENCRYPTION=y
CONFIG_DM_DEFAULT_KEY=y
CONFIG_SCSI_UFSHCD=y
CONFIG_F2FS_FS=y
CONFIG_FS_ENCRYPTION=y
CONFIG_FS_ENCRYPTION_INLINE_CRYPT=y
CONFIG_NETFS_SUPPORT=y
CONFIG_CIFS=y
CONFIG_NLS_UTF8=y
```

ADIOS remains compiled as an optional scheduler. The tree has no
`CONFIG_MQ_IOSCHED_DEFAULT_ADIOS` choice and this update does not make ADIOS
the default.

The pinned hermetic signer is
`kernel_platform/prebuilts/build-tools/linux_musl-x86/bin/openssl`, OpenSSL
1.1.1n, SHA-256
`44618139874abf4a78a3d36ddfe42918b12995d1a69f3a526e78b063ef36e91f`.
All 103 source-built GKI modules use the expected `Build time autogenerated
kernel key`, SHA-512, and PKCS#7. No host OpenSSL fallback was used.

The stock `.500` certificate PEM SHA-256 is
`616201cab78a6f25db2db5ad4353dabd7e2fc3347477f1579d6a9c5ebc8d5b7f`;
its DER SHA-256 is
`f28dbcc60085b21a3cff1342482897fa640b468847473147834f26c4feb2df43`.
Its serial and SHA-256 fingerprint remain
`6E2DD27B61C8671D2C2AFB38E907FE98DC8F0230` and
`F2:8D:BC:C6:00:85:B2:1A:3C:FF:13:42:48:28:97:FA:64:0B:46:88:47:47:31:47:83:4F:26:C4:FE:B2:DF:43`.

The FBE/storage static gate passes: required configuration is unchanged, the
UFS PA_HIBER8TIME path is reconciled without double application, common ABI is
unchanged, and every active stock storage-module import has a matching symbol
CRC. Existing encrypted user0 remains the first physical test.

## Complete stock module contract

| Gate | Result |
|---|---:|
| Modules checked | 1,020 |
| Active compatible | 984 |
| Dormant | 36 |
| Import/CRC edges | 57,216 |
| Active blockers | 0 |
| Unresolved imports | 0 |
| CRC mismatches | 0 |
| Protected-export failures | 0 |
| Signature/trust failures | 0 |
| Structural-provider failures | 0 |
| `rust_binder.ko` | 166/166 imports compatible |
| Stock `system_dlkm` `modules.load` | 82 entries |
| `wwan.ko` position | 28 |

## Immutable stock payload contract

The exact firmware images were re-hashed read-only. None was modified,
repacked, or staged as a candidate:

| Payload | Size | SHA-256 |
|---|---:|---|
| stock `system_dlkm.img` (EROFS) | 14,131,200 | `18f530dcb0e46dc81ede00e18ac4e9b39faf6564fb067f04af9a912d87fd6dd7` |
| stock `vendor_dlkm.img` | 144,105,472 | `157db23ccfa516c7904d4e87614f62c3c99c2dcbb93cc3fc8ccd3388c2cc793f` |
| stock `vendor_boot.img` | 100,663,296 | `3027d80a33fcc65f506d2d909db862657fe28e7eafc6e2af94193d67e6e617eb` |

The stock `system_dlkm` remains EROFS. No DLKM, vendor boot, DTBO, VBMeta,
userdata, metadata, or dynamic-partition image is included in this candidate.

## Artifacts and boot-container proof

The build produced custom development images, but they are not part of this
boot-only stock-DLKM candidate:

| Artifact | Size | SHA-256 |
|---|---:|---|
| previous qualified-parent `Image` | 39,959,040 | `85446aec024214e132de7bf48cb43c6196b2bf80d6fbb719600821364a737828` |
| 6.12.31 `Image` | 39,959,040 | `e69938599cb61fd3ac941a1fc9222db016fdc3726657a33999efc3c2eb57f0d7` |
| generated development `system_dlkm.flatten.ext4.img` | 91,803,648 | `10240ec82a30dca56c943a49864a9b59e9b741d477c7cc24f4db37fd0510a6dc` |
| generated development `vendor_dlkm.img` | 176,680,960 | `fb13a20712a13ab06fda9d93957d6b0d436756f445b80cc4e27971ec573e3e64` |
| staged OOS-compatible `boot.img` | 100,663,296 | `2ae5c754622ca66b60deeb8aee9ea5115f2c56ba28646fbfc2aedf7518706d33` |

The staged image uses Android boot header v4, a zero-byte ramdisk, the exact
fresh 6.12.31 `Image`, the qualified stock `.500` 16-KiB GKI signature object
(SHA-256
`aef79f6617a0005440c038ebb9f9260a983e9b3a414e9103b3576c4524ee574d`),
the qualified salt and firmware AVB properties, and a SHA256_RSA4096 footer.
`avbtool verify_image` passes with public-key SHA-1
`2597c218aae470a130f61162feaae70afd97f011`.

The unpadded AVB image length is 39,981,056 bytes, leaving 60,682,240 bytes
before the configured 100,663,296-byte boot partition limit. Re-extraction
proves that the embedded kernel is byte-identical to the fresh `Image`; the
ramdisk is zero bytes and the natural release is
`6.12.31-android16-5-o-ge42834ea2f51-4k`.

Candidate path:

```text
out/oos1610500-ack-6.12.31-stock-dlkm/boot.img
```

## Warnings

No new fatal warning or validation error remains. Known non-fatal downstream
warning classes persist: DTS schema/style warnings, duplicate BTF type IDs,
SCM-version warnings for separately linked downstream repositories,
deprecated Kleaf APIs, Oplus Python escape warnings, and partial-staging
`depmod` provider warnings for `leds-qcom-flash`, `charger-ulog-glink`,
`qcom-hv-haptics`, `msm_memshare`, `qcom_q6v5`, and `wcd_usbss_i2c`. The
complete 1,020-module provider/import audit is clean; runtime logs still need
comparison during physical testing.

## Physical gate

No flash was performed. The next operation is a controlled boot-only test:
verify a complete backup and rollback image, write only the controlled test
slot's boot partition, verify the full read-back, and attempt one boot. PASS
first requires normal Android, ADB, `sys.boot_completed=1`, existing user0
`RUNNING_UNLOCKED`, FBE/CE access, and no `init_user0_failed`. Full radio,
storage, framework, error-scan, suspend, and two-clean-boot qualification must
follow before publication or promotion.
