# OOS 16.0.10.500 custom ACK 6.12.25 physical validation

## Result

**PASS — OOS 16.0.10.500 CUSTOM ACK 6.12.25 WITH THE EXACT STOCK MODULE
STACK IS PHYSICALLY QUALIFIED**

This result qualifies only the audited 6.12.24 to 6.12.25 boot/Image delta
on the current firmware. It does not qualify a controlled DLKM, authorize a
main update, or import any 6.12.26-or-newer change.

## Tested identity

| Field | Value |
|---|---|
| Device | OnePlus 15 CPH2747 / Canoe |
| Firmware | `CPH2747_16.0.10.500(EX01)` |
| Slot | `_a` |
| Qualified 6.12.24 parent | `42ab0d62d012e2dc54c72fb6eef7014bf9e6d9c6` |
| ACK 6.12.25 integration | `19b41a0ea2d8b24ca0466ed996fe32e10bead86b` |
| ACK 6.12.25 runtime source head | `48618bcd62756eb3dc7e497fc74fb704fbec3a66` |
| Pre-physical documentation head | `f044a9640245ba26cd03da129c6043e0d09c002b` |
| Runtime kernel | `6.12.25-android16-5-o-g48618bcd6275-4k` |
| Tested boot SHA-256 | `494bf88004b13e11379ad3639238f897347d0f8a9b10aa742a553e6c8d812b99` |

The tested image was not rebuilt during physical qualification. The effective
runtime change remains the reviewed Linux/ACK 6.12.24 to 6.12.25 delta and its
Android KMI-preserving integration; no DLKM, vendor_boot, device-tree, VBMeta,
slot-metadata, userdata, or metadata change was introduced.

## Recovery isolation and payload identity

Preflight Android was healthy on the qualified 6.12.24 kernel and its exact
boot hash. TWRP 3.7.1 used the previously qualified hardened helper with
SHA-256 `84b035710c305be859aac72827489850b7d215ee372ab3341be849e051bfab50`.
The CPH2747/Canoe device guard, slot `_a`, no-update snapshot state, partition
capacity, candidate hash, independent backup, helper backup, and boot-only dry
run all passed before the write.

Both complete 100663296-byte rollback backups matched the qualified 6.12.24
boot:

`9e2a7cebe4861d077dc38486b1c8841302ca646880d6bde55dfecb238151d8ad`

Only `boot_a` was written. Its immediate recovery read-back and both Android
read-backs matched the candidate exactly.

| Partition | Physical SHA-256 | Result |
|---|---|---|
| boot_a | `494bf88004b13e11379ad3639238f897347d0f8a9b10aa742a553e6c8d812b99` | candidate input, write, recovery read-back, and Android read-back PASS |
| system_dlkm_a | `18f530dcb0e46dc81ede00e18ac4e9b39faf6564fb067f04af9a912d87fd6dd7` | exact stock, unchanged |
| vendor_dlkm_a | `157db23ccfa516c7904d4e87614f62c3c99c2dcbb93cc3fc8ccd3388c2cc793f` | exact stock, unchanged |
| vendor_boot_a | `3027d80a33fcc65f506d2d909db862657fe28e7eafc6e2af94193d67e6e617eb` | exact stock, unchanged |

The native system load contract remained 82 entries with `wwan.ko` at entry
28. No rollback was required.

## Boot and existing encrypted user0

| Gate | Result |
|---|---|
| First Android boot | PASS |
| Second clean Android boot | PASS |
| Existing user0 | `RUNNING_UNLOCKED` on both boots |
| Metadata-encrypted mount | PASS |
| `vdc cryptfs enablefilecrypto` | exited with status 0 |
| `vdc cryptfs init_user0` | exited with status 0 |
| `fscrypt_init_user0` | completed successfully |
| `init_user0_failed` | NOT OBSERVED |
| Rescue Party / recovery redirect | NOT OBSERVED |

The existing encrypted data was retained without a format, metadata wipe,
encryption-policy change, FBE disabler, or policy weakening.

## Physical regression results

| Subsystem | Result |
|---|---|
| WLAN | PASS; 6135 MHz WPA3-SAE, off/on recovery, numeric IP and DNS 5/5 |
| Cellular | PASS; Visible LTE HOME/IN_SERVICE, RMNET IPv4/IPv6 addresses and routes, stabilized IPv4/IP/DNS traffic |
| Public IPv6 | RMNET addresses and policy routes present; the bounded direct public probe had no reachable route, matching the prior non-blocking provisioning observation |
| Handoff | PASS; one bounded WLAN-to-cellular-to-WLAN cycle; one first cellular numeric packet was lost during route stabilization, then traffic passed |
| Bluetooth | PASS; off/on and existing HID reconnect |
| NFC | PASS; service on, Wallet launch, HCE registration, and eSE1 route |
| Camera | PASS; Oplus camera captured `IMG20260827174655.heic` |
| Audio | PASS; ringtone played through the speaker and the user confirmed clear sound |
| USB | PASS; `mtp,adb` and continuous ADB enumeration |
| Graphics/UI | PASS; Wallet, camera, file manager, and launcher rendered normally |
| Deep idle/resume | 5/5 PASS |
| Framework | PASS; system_server ready and critical services remained alive |

The point-release isolation used one bounded network handoff rather than a
repeated 25-cycle Wi-Fi gate. The primary boundary was truthful 6.12.25 boot,
existing encrypted user0, and compatibility with the exact stock firmware
module stack.

## Kernel, module, and framework scan

The final scans found no use-after-free, KASAN/UBSAN report, kernel BUG, Oops,
panic, refcount failure, double free, invalid pointer, list corruption, RCU
stall, unresolved symbol, CRC/version disagreement, signature failure,
`init_user0_failed`, or critical framework crash loop.

The Oplus touch-HBP `frame_put()` fortify warning and the OEM blocked-thread
diagnostics emitted during forced deep idle match the physically qualified B08
warning classes. No new fatal or point-release-specific warning class was
observed.

Physical evidence, including both rollback images and complete boot/log scans,
is retained under the ignored local artifact path:

`out/oos1610500-ack-6.12.25-stock-dlkm/physical-20260827/`

## Decision

**PASS — OOS 16.0.10.500 CUSTOM ACK 6.12.25 WITH EXACT STOCK DLKMS IS
PHYSICALLY QUALIFIED**

The exact state may be frozen as
`feature/oos1610500-ack-6.12.25-qualified` and tagged
`oos16.0.10.500-ack-6.12.25-qualified`. ACK 6.12.26 must not begin until both
remote refs are pushed and verified. Main remains unchanged.
