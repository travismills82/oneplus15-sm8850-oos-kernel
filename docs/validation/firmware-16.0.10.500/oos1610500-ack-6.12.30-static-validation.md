# OOS 16.0.10.500 ACK 6.12.30 static validation

## Result

**PASS — READY FOR A CONTROLLED BOOT-ONLY USER0 PHYSICAL TEST.**

The candidate passes the complete static compatibility gate. It has not been
flashed, pushed, merged, released, or physically qualified.

| Field | Value |
|---|---|
| Branch | `experiment/oos1610500-ack-6.12.30-from-6.12.29` |
| Qualified parent | `a42551e7ea7d487ab0dffc53a8310b073477a080` |
| Complete local 6.12.30 replay | `fc4b7c3821fdd027531439f559794094b9737fb8` |
| Android KABI repair / built source | `620f76c23662ce9239d2a7abe3073cb0f109cd08` |
| Natural kernel release | `6.12.30-android16-5-o-g620f76c23662-4k` |
| KMI generation | 5 |
| Firmware contract | CPH2747 OxygenOS 16.0.10.500(EX01) |

## Build and ABI/KMI gates

The following commands completed successfully from `kernel_platform/`:

```text
tools/bazel build //soc-repo:canoe_perf_dist
tools/bazel build --kbuild_symtypes //common:kernel_aarch64
tools/bazel build \
  //common:kernel_aarch64_abi \
  //common:kernel_aarch64_abi_kmi_symbol_checks \
  //common:kernel_aarch64_abi_diff
tools/bazel run //common:kernel_aarch64_abi_dist -- \
  --destdir=out/oos1610500-ack-6.12.30-compat-abi-dist-20260903-184620
tools/bazel build //soc-repo:canoe_perf_abi
tools/bazel run //soc-repo:canoe_perf_dist
```

| Gate | Result |
|---|---|
| Canoe distribution build | PASS |
| Explicit symtypes | PASS; 1,398 seconds |
| Enforced common ABI | PASS |
| Strict KMI symbol list | PASS |
| Common ABI targets | PASS |
| Canoe device-inclusive ABI dump | PASS; 4,084 actions |
| Dist execution and copy | PASS; 4,600-action graph, 25 actions executed |

The Canoe ABI target is a device-inclusive dump, not an enforced device ABI
diff. The retained-stock binary audit below is the authoritative device-module
gate. `kernel_aarch64_Module.symvers` is byte-for-byte identical to the
qualified 6.12.29 state, SHA-256
`6889e56eb427705a9002c7b030b2af7b9a6153c874efb1cda5ddaf56be60aa27`.

Known non-fatal downstream warning classes remain: DTS schema/style warnings,
duplicate BTF type IDs, SCM-version warnings for non-common modules,
deprecated Kleaf APIs, Oplus Python escape warnings, and partial-staging
`depmod` provider warnings for `leds-qcom-flash`, `charger-ulog-glink`,
`qcom-hv-haptics`, `msm_memshare`, `qcom_q6v5`, and `wcd_usbss_i2c`. The
complete stock provider/import audit is clean; runtime logs still require
comparison during physical testing.

## Configuration, FBE, and signing

The generated 6.12.29 and 6.12.30 Canoe configurations differ only in the
generated version comment. No effective `CONFIG_` symbol changed.

```text
CONFIG_MODVERSIONS=y
CONFIG_GENDWARFKSYMS=y
CONFIG_MODULE_SIG=y
CONFIG_MODULE_SIG_SHA512=y
CONFIG_MODULE_SIG_HASH="sha512"
CONFIG_BLK_INLINE_ENCRYPTION=y
CONFIG_DM_DEFAULT_KEY=y
CONFIG_SCSI_UFSHCD=y
CONFIG_F2FS_FS=y
CONFIG_FS_ENCRYPTION=y
CONFIG_FS_ENCRYPTION_INLINE_CRYPT=y
CONFIG_NETFS_SUPPORT=y
CONFIG_CIFS=y
CONFIG_NLS_UTF8=y
```

`CONFIG_WQ_POWER_EFFICIENT_DEFAULT` remains unset and ADIOS has no effective
symbol in both the qualified 6.12.29 config and this candidate. The ACK-only
update does not silently alter those pre-existing feature states.

The pinned hermetic signer remains
`kernel_platform/prebuilts/build-tools/linux_musl-x86/bin/openssl`, OpenSSL
1.1.1n, SHA-256
`44618139874abf4a78a3d36ddfe42918b12995d1a69f3a526e78b063ef36e91f`.
All 103 generated GKI modules use the expected `Build time autogenerated
kernel key`, SHA-512, and PKCS#7. No host OpenSSL fallback was used.

The stock `.500` certificate PEM SHA-256 is
`616201cab78a6f25db2db5ad4353dabd7e2fc3347477f1579d6a9c5ebc8d5b7f`;
its DER SHA-256 is
`f28dbcc60085b21a3cff1342482897fa640b468847473147834f26c4feb2df43`.
Its serial and SHA-256 fingerprint remain
`6E2DD27B61C8671D2C2AFB38E907FE98DC8F0230` and
`F2:8D:BC:C6:00:85:B2:1A:3C:FF:13:42:48:28:97:FA:64:0B:46:88:47:47:31:47:83:4F:26:C4:FE:B2:DF:43`.

The Android KABI repair restores the qualified two-argument
`virtqueue_reset()` contract. Together with identical `Module.symvers`, empty
common ABI reports, and the full stock-module audit, no module-observable
structure or symbol-CRC change remains. FBE/storage passes static review;
encrypted existing user0 remains the first runtime oracle.

## Complete stock module contract

| Gate | Result |
|---|---:|
| Modules checked | 1,020 |
| Active compatible | 984 |
| Dormant | 36 |
| Import/CRC edges | 57,216 |
| Active blockers | 0 |
| Unresolved imports | 0 |
| CRC mismatches | 0 |
| Protected-export failures | 0 |
| Signature/trust failures | 0 |
| Structural-provider failures | 0 |
| `rust_binder.ko` | 166/166 imports compatible |
| Stock `system_dlkm` `modules.load` | 82 entries |
| `wwan.ko` position | 28 |

## Immutable stock payload contract

The exact firmware images were re-hashed read-only. None was modified,
repacked, or staged as a candidate:

| Payload | Size | SHA-256 |
|---|---:|---|
| stock `system_dlkm.img` (EROFS) | 14,131,200 | `18f530dcb0e46dc81ede00e18ac4e9b39faf6564fb067f04af9a912d87fd6dd7` |
| stock `vendor_dlkm.img` | 144,105,472 | `157db23ccfa516c7904d4e87614f62c3c99c2dcbb93cc3fc8ccd3388c2cc793f` |
| stock `vendor_boot.img` | 100,663,296 | `3027d80a33fcc65f506d2d909db862657fe28e7eafc6e2af94193d67e6e617eb` |

## Artifacts and boot-container proof

The build produced development-only custom DLKM images, but they are not part
of this boot-only stock-DLKM candidate:

| Artifact | Size | SHA-256 |
|---|---:|---|
| `Image` | 39,954,944 | `916d380814caccb9c2b76f0c779fe6982c2c9288a9f91646a24f040bfffcbc5f` |
| generated `system_dlkm.flatten.ext4.img` | 91,795,456 | `86ff28127b649b25ae0da17fb39c904c6f8580d6379efcb79c9ed9eab796f76f` |
| generated `vendor_dlkm.img` | 176,730,112 | `d07d3bdd80824b82a9820aa2b4ab93e035d8f87c391940de29bb0d991a16ddc2` |
| staged OOS-compatible `boot.img` | 100,663,296 | `667ea8aaeae99006b54d62bb02dac6eeb733712dd6fd53a3bc9bdf1b1e249584` |

The staged image uses Android boot header v4, a zero-byte ramdisk, the exact
fresh 6.12.30 `Image`, the qualified stock `.500` 16-KiB GKI signature object
(SHA-256
`aef79f6617a0005440c038ebb9f9260a983e9b3a414e9103b3576c4524ee574d`),
the qualified salt and AVB properties, and a SHA256_RSA4096 footer.
`avbtool verify_image` passes with public-key SHA-1
`2597c218aae470a130f61162feaae70afd97f011`.

The unpadded AVB image length is 39,976,960 bytes, leaving 60,686,336 bytes
before the configured 100,663,296-byte boot partition limit. Re-extraction
shows that the embedded kernel is byte-identical to the fresh `Image` and
reports `6.12.30-android16-5-o-g620f76c23662-4k`.

Candidate path:

```text
out/oos1610500-ack-6.12.30-stock-dlkm/boot.img
```

## Physical gate

No flash was performed. The next operation is a controlled boot-only test:
verify a complete backup and rollback image, write only the controlled test
slot's boot partition, verify the full read-back, and attempt one boot. PASS
first requires normal Android, ADB, `sys.boot_completed=1`, existing user0
`RUNNING_UNLOCKED`, FBE/CE access, and no `init_user0_failed`. Full radio,
storage, framework, error-scan, suspend, and two-clean-boot qualification must
follow before publication or promotion.
