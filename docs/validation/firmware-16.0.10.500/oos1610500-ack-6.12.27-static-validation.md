# OOS 16.0.10.500 ACK 6.12.27 static validation

## Result

The controlled 6.12.27 candidate passes every static gate required before a
boot-only physical test. It is not physically qualified yet.

- branch: `experiment/oos1610500-ack-6.12.27`;
- qualified parent: `8c18383461dfef341c9395b9fbc95a795a698a21`;
- source head used for the build:
  `20d91bf4ec43f6171bab445c4123350e64ab0883`;
- natural kernel release:
  `6.12.27-android16-5-o-g20d91bf4ec43-4k`;
- KMI generation: 5;
- firmware: CPH2747 OxygenOS 16.0.10.500(EX01).

The official 6.12.27 interval remains two individually reviewable commits:

1. `793d3faa9246539c008a89570b0e0357e6a699f3` —
   `bpf: Fix BPF_INTERNAL namespace import`;
2. `fa3c82a7f526c9f81483599ce80034154cbed316` —
   `Linux 6.12.27`.

The separate `20d91bf4ec43` commit contains provenance documentation only. No
aggregate ACK source commit was created.

## Builds and ABI/KMI

The following commands completed successfully from `kernel_platform/`:

```text
tools/bazel query //soc-repo:canoe_perf_dist
tools/bazel build //soc-repo:canoe_perf_dist
tools/bazel build --kbuild_symtypes //common:kernel_aarch64
tools/bazel run //common:kernel_aarch64_abi_dist -- \
  --destdir=out/oos1610500-ack-6.12.27-abi-dist
tools/bazel build \
  //common:kernel_aarch64_abi \
  //common:kernel_aarch64_abi_kmi_symbol_checks \
  //common:kernel_aarch64_abi_diff
tools/bazel build //soc-repo:canoe_perf_abi
tools/bazel run //soc-repo:canoe_perf_dist
```

The enforced common ABI report is empty. Strict KMI symbol-list checks pass.
The Canoe ABI dump builds; as documented by this repository, that target is a
device-inclusive dump and is not represented as an enforced device ABI diff.

The only generated `.config` difference from the physically qualified 6.12.26
build is the generated version comment changing from 6.12.26 to 6.12.27.
There is no semantic configuration change. Module signing, SHA-512 signing,
MODVERSIONS, GENDWARFKSYMS, protected-module policy, and unused-symbol trimming
remain enabled.

Known downstream BTF duplicate-type, DTS schema, Kleaf deprecation, Oplus
Python syntax, SCM-version, and generated-module `depmod` warnings are
unchanged in class from the qualified parent. The authoritative retained-stock
provider audit below is clean.

## Exact stock module contract

The exact stock OxygenOS 16.0.10.500 module universe was audited against the
candidate `Module.symvers` and the existing stock load policy:

| Gate | Result |
|---|---:|
| Modules checked | 1,020 |
| Active compatible | 984 |
| Dormant | 36 |
| Import/CRC edges | 57,216 |
| Active blockers | 0 |
| Unresolved imports | 0 |
| CRC mismatches | 0 |
| Protected-export failures | 0 |
| Signature/trust failures | 0 |
| `rust_binder` | 166/166 imports compatible |
| Stock `system_dlkm` `modules.load` | 82 entries |
| `wwan.ko` position | 28 |

The candidate `Module.symvers` is byte-identical to the qualified 6.12.26
`Module.symvers`, SHA-256
`6889e56eb427705a9002c7b030b2af7b9a6153c874efb1cda5ddaf56be60aa27`.

The immutable current-firmware payload hashes remain:

| Payload | SHA-256 |
|---|---|
| stock `system_dlkm.img` | `18f530dcb0e46dc81ede00e18ac4e9b39faf6564fb067f04af9a912d87fd6dd7` |
| stock `vendor_dlkm.img` | `157db23ccfa516c7904d4e87614f62c3c99c2dcbb93cc3fc8ccd3388c2cc793f` |
| stock `vendor_boot.img` | `3027d80a33fcc65f506d2d909db862657fe28e7eafc6e2af94193d67e6e617eb` |

None was modified or repackaged.

## Boot container

The current packager was first proven by reproducing the physically qualified
6.12.26 boot byte-for-byte, including SHA-256
`6a954d968b784886cf8c1dcf5b500fa15129b782e5056b9831af0f0b1ff47099`.

The 6.12.27 candidate then replaced only the kernel payload while preserving
the exact stock `.500` 16 KiB GKI signature object, stock firmware AVB
properties and salt, Android boot header v4 layout, and 100,663,296-byte boot
partition geometry. Re-extraction proves the embedded kernel equals the fresh
Image; AVB footer and payload verification pass.

| Artifact | Size | SHA-256 |
|---|---:|---|
| `Image` | 39,954,944 | `ec61bb30e070b7eda06d4dc9607314f44f75b489eb970410e94546c2bfadb4b5` |
| OOS-compatible `boot.img` | 100,663,296 | `8b5753c49a3899c0635558584ef6814e927662b459ecb4233761d532faad15b5` |
| stock `.500` GKI signature object | 16,384 | `aef79f6617a0005440c038ebb9f9260a983e9b3a414e9103b3576c4524ee574d` |

The unpadded AVB image size is 39,976,960 bytes, leaving 60,686,336 bytes
before the configured boot partition limit. The local candidate is staged at:

```text
out/oos1610500-ack-6.12.27-stock-dlkm/boot.img
```

## Physical gate

Status: **READY FOR CONTROLLED USER0 PHYSICAL TEST**.

The device was not connected when the static gate completed, so no flash or
runtime claim is made. The first test must start from the exact qualified
6.12.26 state, back up and hash-verify `boot_a`, write only the candidate
`boot_a`, verify the full partition read-back, and require existing user 0 to
reach `RUNNING_UNLOCKED` without `init_user0_failed`. Full radio, storage,
suspend, framework, error-scan, and two-clean-boot qualification follows only
after that oracle passes.
