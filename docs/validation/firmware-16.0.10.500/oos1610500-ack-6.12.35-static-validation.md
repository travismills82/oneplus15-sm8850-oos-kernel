# OOS 16.0.10.500 ACK 6.12.35 static validation

## Result

**PASS — ACK 6.12.35 is statically compatible with the retained OxygenOS
16.0.10.500 boot-only contract.**

- branch: `experiment/oos1610500-ack-6.12.35-from-6.12.32`;
- 6.12.32 source parent: `989ec6e0ed86738d48ef35d9a69a22065f55dafb`;
- tested source head: `b42c1af35b26f7d2e1b0e6c8e9eaaf3628c7e32e`;
- natural kernel release:
  `6.12.35-android16-5-o-gb42c1af35b26-4k`;
- KMI generation: 5;
- firmware: CPH2747 OxygenOS 16.0.10.500(EX01).

## Commit-by-commit ACK ancestry

Every imported stable/ACK change remains an individual Git commit with its
upstream author, subject, and dependency order. No aggregate ACK commit was
created.

| Interval | Official stable commits | Separate Android KABI repairs |
|---|---:|---:|
| 6.12.32 to 6.12.33 | 25 | 1 |
| 6.12.33 to 6.12.34 | 506 | 4 |
| 6.12.34 to 6.12.35 | 414 | 1 |

The local milestone commits are:

- `827e3ade0deeef8167d55bf6ccafcc02d9ffdf8d` — `Linux 6.12.33`;
- `4bb9fbe77a97818853e660713a0fda3f830793cb` — `Linux 6.12.34`;
- `619d165aefd31e92f9ffab1f858e1c290f6a0724` — `Linux 6.12.35`.

The KABI repairs preserve the Android generation-5 binary-module contract for
UCSI, page-pool DMA tracking, device-mapper zones, PCI endpoint BAR layout,
Bluetooth MGMT locking, and TCP receive-queue accounting. They are separate
commits following the stable interval in which each incompatibility appears.
No 6.12.36-or-newer stable commit is present.

## Build and ABI/KMI

The following completed successfully from `kernel_platform/`:

```text
tools/bazel build //soc-repo:canoe_perf_dist
tools/bazel build --kbuild_symtypes //common:kernel_aarch64
tools/bazel build \
  //common:kernel_aarch64_abi \
  //common:kernel_aarch64_abi_kmi_symbol_checks \
  //common:kernel_aarch64_abi_diff
tools/bazel run //common:kernel_aarch64_abi_dist -- \
  --destdir=out/oos1610500-ack-6.12.35-abi-dist-20260905-073025
tools/bazel build //soc-repo:canoe_perf_abi
tools/bazel run //soc-repo:canoe_perf_dist
```

The enforced common ABI reports are empty and strict KMI symbol-list checks
pass. The Canoe device-module-inclusive ABI dump also builds; it is not
misrepresented as an enforced device ABI diff.

The final `Module.symvers` has 9,356 records and is byte-identical to the
previously qualified stock-module provider contract:

```text
6889e56eb427705a9002c7b030b2af7b9a6153c874efb1cda5ddaf56be60aa27
```

The earlier 1,020-module extraction was removed during disk cleanup and was
not redundantly extracted again. Provider CRC identity proves that its 51,105
vmlinux-served stock import edges are unchanged; the unchanged stock binaries
retain their stock-to-stock edges and signatures. Runtime module loading was
then checked on the physical device with no signature, CRC, vermagic,
protected-export, or unresolved-symbol failure.

## Configuration and artifacts

The generated Canoe `.config` retains:

```text
CONFIG_WQ_POWER_EFFICIENT_DEFAULT=y
CONFIG_MQ_IOSCHED_ADIOS=y
CONFIG_NETFS_SUPPORT=y
CONFIG_CIFS=y
CONFIG_NLS_UTF8=y
CONFIG_MODULE_SIG=y
CONFIG_MODULE_SIG_SHA512=y
CONFIG_MODULE_SIG_HASH="sha512"
CONFIG_MODVERSIONS=y
CONFIG_GENDWARFKSYMS=y
```

ADIOS remains selectable and is not the active default; UFS reports
`mq-deadline` selected with Kyber, ADIOS, and BFQ available. Btrfs and NFS
remain disabled.

| Artifact | Size | SHA-256 |
|---|---:|---|
| `Image` | 39,959,040 | `cc4a7933fc936c78d63d347da2022795a439c49cd1b9e250cd4a5a09f7e27238` |
| OOS-compatible `boot.img` | 100,663,296 | `bbf3e9ed0fae1e55b3c7522cadff9509decc24bfd5a412ae66d4cbebea5effdc` |
| `Module.symvers` | 575,272 | `6889e56eb427705a9002c7b030b2af7b9a6153c874efb1cda5ddaf56be60aa27` |

The Android boot header v4, embedded Image identity, stock `.500` 16 KiB GKI
signature tail, SHA256_RSA4096 AVB footer, firmware properties, and exact
100,663,296-byte partition geometry all verify. Generated custom DLKM images
remain development artifacts and are not part of this boot-only release.
