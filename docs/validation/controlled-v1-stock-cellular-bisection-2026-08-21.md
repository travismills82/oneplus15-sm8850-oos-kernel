# Controlled-v1 stock-cellular bisection — 2026-08-21

## Scope and safety boundary

This experiment keeps the controlled-v1 Image fixed and restores the complete
OxygenOS 16.0.9.400(EX01) vendor-side cellular packet-data closure to exact
stock binaries. It does not change WLAN source versions, `CONFIG_CFG80211`,
module-signature enforcement, MODVERSIONS, protected exports, `vendor_boot`,
or VBMeta. The ordered physical bisection below changed only the named slot
`_b` payload at each stage and verified every write by full read-back SHA-256.

Slot `_b` now runs the physically validated TEST 3 stack. Slot `_a` remains
marked unbootable and is not a fallback. Every physical write below was gated
by a fresh, readable, SHA-256-verified host backup and a second persistent TWRP
backup of the target partition.

## Pass/fail evidence

The failed controlled-v1 and restored r7 captures agree through the observable
kernel-side IPA/GSI/RMNET initialization sequence:

- IPA readiness callback registration;
- GSI device registration;
- RMNET control, low-latency, and netdev PM-client registration;
- `rmnet_data1` registration to IPA;
- Peach-v2's IPA-ready callback and TX-buffer mapping.

The first proven module-delivery divergence is `wwan`: restored r7 has it in
`/proc/modules`, while the failed stack does not. Stock system-DLKM
`modules.load` explicitly requests `wwan.ko`; the failed controlled image
contains a matching controlled `wwan.ko` but shipped an empty `modules.load`.

The first meaningful functional divergence is then the RIL `SETUP_DATA_CALL`
response. Restored r7 receives `NONE(0x0)` with an RMNET interface, address,
DNS, and gateway. Controlled-v1 receives `OEM_DCFAILCAUSE_4(0x1004)` with
`cid=-1` and empty interface/address/DNS/gateway fields. No authoritative
mapping for that vendor code exists in the available kernel/vendor source;
unrelated numeric uses of `0x1004` are not a definition of the RIL failure.

The chronological comparison is in
`docs/validation/cellular-pass-fail-timeline.tsv`. Module hash, signer,
vermagic, load-position, and dependency evidence is generated by
`tools/audit-controlled-v1-cellular-bisection.py`.

## Cellular data closure

Walking real `depends=` metadata backward from GSI/IPA/RMNET/data roots finds
63 modules across `vendor_boot`, `vendor_dlkm`, and `system_dlkm`. The full
table is `docs/validation/cellular-data-closure.tsv`.

Candidate A preserves all 27 vendor-DLKM members of this closure byte-for-byte
from stock. Across the original candidate partitions, 62 of 63 closure members
remain exact stock; the sole binary difference is controlled modular
`wwan.ko`. Its load-policy correction is deliberately isolated by TEST 2
before Candidate A can be tested.

The prior failed vendor image had replaced or re-signed IPA/IPAM/RMNET-related
modules. Candidate A restores those exact stock binaries rather than assuming
that clean symbol/CRC checks guarantee behavioral equivalence.

## Minimum controlled WLAN closure

The smallest statically valid controlled WLAN set is:

| Module | Candidate action | Reason |
| --- | --- | --- |
| `cfg80211` | source replacement | Controlled-v1 modular provider required by the source Peach module. |
| `mac80211` | source replacement | Matching controlled system/WLAN contract and shared protected-provider boundary. |
| `qca_cld3_peach_v2` | source replacement | Active Canoe WLAN implementation. |
| `qca_cld3_kiwi_v2` | re-sign exact stock payload | Dormant stock consumer shares controlled cfg80211 exports; signing closure requires a valid project signature. |
| `qca_cld3_wcn7750` | re-sign exact stock payload | Dormant stock consumer shares controlled cfg80211 exports; signing closure requires a valid project signature. |
| `wonder` | re-sign exact stock payload | Stock payload is retained; its cfg80211 consumer contract requires the controlled-v1 signature closure. |

No source-built CNSS, IPA, GSI, RMNET, QMI, SMEM-mailbox, or firmware-service
vendor module is needed in Candidate A. The source-built Peach module imports 28 IPA
symbols from exact stock `ipam.ko`; every expected CRC matches. Its other
stock-provider contracts also match: `cnss2` (67 symbols), `cnss_nl` (3),
`cnss_prealloc` (3), `cnss_utils` (8), `qmi_helpers` (8),
`qcom_va_minidump` (3), and `rproc_qcom_common` (2).

Therefore the static answer is **YES**: source-built Peach-v2 can link and load
against the exact stock IPA/GSI/RMNET and CNSS/QMI provider set. Physical Wi-Fi
and cellular coexistence remains unproven until TEST 3.

On this exact OxygenOS inventory, `cfg80211.ko` and `mac80211.ko` are delivered
from vendor-DLKM, not system-DLKM. Candidate A preserves that ownership and
does not introduce duplicate module names across partitions. Both remain
modular (`CONFIG_CFG80211=m`, `CONFIG_MAC80211=m`).

## Candidate A

Candidate vendor image:

```text
out/cellular-bisection-20260821/candidate-a-vendor/vendor_dlkm.img
SHA-256: 24e66015a3e4ea3583f895d529008d8c7c3706d7bc506ef550df936935127b80
size: 143,986,688 bytes
```

It begins with the exact verified stock vendor-DLKM tree (436 modules) and
changes only the six modules above. The source modules have import/export CRC
contracts matching stock; each re-signed module is the complete exact stock
module byte sequence followed by a new PKCS#7 signature. All six use the
controlled-v1 signer.

Dependency reconciliation pruned 53 stale edges to implementations now built
into Image and retained 37 real controlled-system edges. The generated ext4
image passed `e2fsck`; all six module readbacks, filesystem metadata, SELinux
labels, hashes, and module metadata were verified. A partition-local AVB
hashtree/FEC footer was regenerated; no parent VBMeta image was changed.

Supporting reports are under
`out/cellular-bisection-20260821/candidate-a-vendor/`:

- `module-replacement.tsv`
- `cellular-stock-module-list.tsv`
- `wlan-custom-module-list.tsv`
- `dependency-report.tsv`
- `CRC-report.tsv`
- `signature-report.tsv`
- `SHA256SUMS`

### Corrected controlled system-DLKM

The original controlled system-DLKM contained 46 matching signed modules but
an empty `modules.load`. A packaging-only repack restores stock order filtered
to those 46 surviving modules, including `wwan.ko` at entry 21. It does not
change Image or any module binary:

```text
out/cellular-bisection-20260821/candidate-system-load-policy-final2/system_dlkm.img
SHA-256: edebc94818e6fa4e214d58fd82fe46f6c513fc9850b3e7b77caf076a12270f05
size: 45,658,112 bytes
module payload changes: 0
modules.load entries: 46
```

The EROFS filesystem/xattrs, readback module hashes, `modules.load` readback,
and partition-local AVB footer validate. A second clean repack produced the
same complete image SHA-256. Parent VBMeta remains unchanged. This
repack is produced by `tools/stage-controlled-system-dlkm-load-policy.sh` and
uses the fixed controlled-v1 Image/module release contract.

## Static gates

The controlled-v1 build was revalidated through the fail-closed signing
wrapper with source identity
`090459863b8ccf45599c6506461d144ab736d9a5`. A direct Bazel invocation without
the declared signing repository failed as designed rather than substituting
an autogenerated key. The declared-key invocation then produced:

- `//soc-repo:canoe_perf_dist`: PASS;
- `//common:kernel_aarch64_abi`: PASS;
- `//common:kernel_aarch64_abi_kmi_symbol_checks`: PASS;
- `//common:kernel_aarch64_abi_diff`: PASS;
- all five generated ABI reports and the ABI error file: 0 bytes;
- kernel release: `6.12.23-android16-5-o-g090459863b8c-4k`;
- generated config SHA-256:
  `b53d48b303059adb49a8dbe457145a4b7523a77fae621ea8d9e7b0b727e1615b`;
- `Module.symvers` SHA-256:
  `de57709f3de38afb3e266481da09433687979ffb88ee607bda93ac4732dd7e0b`;
- `System.map` SHA-256:
  `e2dfd062ea06fbbde98cc863e1fce7451df65315a0f56a9b913c3e7c9f281ed5`;
- both reviewed stock-OOS and controlled-v1 certificates present in
  `vmlinux`.

The clean rebuilt `Image` and the exact controlled-v1 Image extracted from
the physically tested boot payload are both 47,962,624 bytes. They differ in
exactly 20 consecutive bytes at file offsets 33,014,000–33,014,019: the GNU
build ID changed from
`2da94b28d0846d0b8a1a8f1cbd4b83f147a5ae0f` to
`725998e246cde504511d2472025b319fd1488644`. Masking only those 20 bytes gives
the same SHA-256 for both Images:
`cd5f51e065499ea1f5399254a43203b317e92c8f625e5045dde1cfcd4f1d9f72`.
Their embedded Linux version, toolchain identity, release string, and build
timestamp are identical. TEST 1–3 therefore continue to use the exact
controlled-v1 boot payload SHA-256
`25efe5463938757339dcfada56ee47d77d3c0cc42b6707dda7dd1613c20fc313`;
the diagnostic rebuild is not substituted into the bisection.

Candidate validation additionally proves:

- 436 vendor-DLKM modules present;
- 0 missing required stock modules;
- 0 unresolved imports;
- 0 CRC mismatches;
- 0 protected-export closure failures;
- retained-stock contract: 700 compatible, 209 dormant, 0 blockers;
- 27/27 vendor-side cellular closure modules exact stock;
- 195/195 cfg80211 imports, 161/161 exports matched;
- 319/319 mac80211 imports, 142/142 exports matched;
- 602/602 Peach-v2 imports matched;
- `CONFIG_CFG80211=m`, `CONFIG_MAC80211=m`, and `CONFIG_RFKILL=y` in the generated Canoe config.

The vendor-boot copies of `mac80211` and `wonder` remain dormant and forbidden
from supported load lists. They are not treated as active providers.

## Physical bisection

| Test | Image | system_dlkm | vendor_dlkm | Purpose | Current result |
| --- | --- | --- | --- | --- | --- |
| TEST 0 | r7 `86eba62f…7b38d1f` | stock `18f530dc…fd6dd7` | stock `40d4bd03…330e6` | Known pass | PASS: LTE home, RMNET IPv4/IPv6/default route, IP and DNS 5/5; Wi-Fi pass. |
| TEST 1 | controlled-v1 `25efe546…fc313` | exact stock | exact stock | Isolate Image/signing/release identity | **PHYSICAL PASS**: controlled release booted; Visible LTE HOME; `rmnet_data2` IPv4+IPv6; cellular default routes; `NONE(0x0)`; IP and DNS 5/5. |
| TEST 2 | controlled-v1 | corrected controlled-v1 `edebc948…70f05` | exact stock | Isolate controlled system providers and the restored `wwan` load policy | **CELLULAR PHYSICAL PASS**: `wwan` loaded; Visible LTE HOME; `rmnet_data2` IPv4+IPv6; default route; `NONE(0x0)`; IP and DNS 5/5. Stock WLAN did not complete the controlled provider handoff, as expected before Candidate A. |
| TEST 3 | controlled-v1 | corrected controlled-v1 | Candidate A `24e66015…27b80` | Minimum source WLAN plus exact stock cellular vendor closure | **PHYSICAL PASS**: source Peach/cfg80211/mac80211 loaded; Wi-Fi 5/6 GHz WPA3 and reload passed; Visible LTE HOME and cellular IP/DNS passed; simultaneous Wi-Fi/RMNET state verified. |

The tests ran in this order and isolated the original failure. TEST 1 proves
the controlled-v1 Image/signing/release identity does not break cellular.
TEST 2 proves the corrected controlled system-DLKM and restored `wwan` load
policy do not break cellular. TEST 3 proves the minimum controlled WLAN set can
run against the exact stock IPA/GSI/RMNET/CNSS/QMI provider set while cellular
packet data remains functional.

### Backup and write verification

- Before TEST 1, `boot_b` was backed up with SHA-256
  `86eba62f4f93f02aaacda89ef903c91a3e531575aba1cf253ce70e0887b38d1f`.
  Controlled `boot_b` read back as
  `25efe5463938757339dcfada56ee47d77d3c0cc42b6707dda7dd1613c20fc313`.
- Before TEST 2, the full 88,514,560-byte stock `system_dlkm_b` logical
  partition was backed up as
  `8d7b2766102fe1f96991f5d00bcf1a9eb47c0c7fc118a25137cf05761f31cbf3`.
  The candidate image prefix read back as
  `edebc94818e6fa4e214d58fd82fe46f6c513fc9850b3e7b77caf076a12270f05`.
- Before TEST 3, the full 143,986,688-byte stock `vendor_dlkm_b` was backed
  up byte-for-byte as
  `40d4bd03e9d315aac562234019f6db192617bb1ab65532157b81022ebc7330e6`.
  The persistent TWRP backup is
  `/sdcard/TWRP/kernel-flash-backups/controlled-stack-b-20260821-171402/`.
  Candidate A read back as
  `24e66015a3e4ea3583f895d529008d8c7c3706d7bc506ef550df936935127b80`.
- `vendor_boot`, VBMeta, and active-slot metadata were not changed.

The hardened TWRP helper accepted decrypted persistent storage, rejected no
safety gate, reported no active snapshot state, and verified every backup and
post-write hash. Slot `_a` was never used or modified and remains unbootable.

### TEST 3 runtime proof

The running release is
`6.12.23-android16-5-o-g090459863b8c-4k`. Runtime module state contains the
controlled `qca_cld3_peach_v2`, `cfg80211`, and `mac80211` modules plus the
re-signed exact-stock `wonder`; the active IPA/GSI/RMNET/CNSS/QMI providers are
the exact stock binaries retained by Candidate A.

Wi-Fi first associated at 5220 MHz and passed IP and DNS 5/5. An OFF/ON reload
then associated at 6135 MHz with WPA3-SAE and again passed IP and DNS 5/5.
With Wi-Fi disabled, Visible registered LTE HOME and `rmnet_data2` received
IPv4 `100.73.131.228/29` plus a global IPv6 address. Android installed IPv4 and
IPv6 default routes, the RIL data call returned `NONE(0x0)`, and both
`1.1.1.1` and `google.com` passed 5/5. After Wi-Fi was re-enabled, the 6135 MHz
WPA3 connection and the live RMNET addresses/routes coexisted.

The final targeted scan found zero unknown-symbol, MODVERSION/vermagic,
module-signature, protected-export, or cellular setup-data-call failures. The
boot log retains pre-existing Oplus driver warnings/call traces also present
on the stock/TEST 1 path; none identifies the controlled WLAN or stock cellular
closure as its origin.

## Current conclusion

The original controlled-v1 cellular failure is outside the Image/signing
boundary. Its first proven delivery defect was the empty system-DLKM
`modules.load`, which omitted `wwan` at runtime; corrected TEST 2 passed. The
broader source-built/re-signed vendor cellular replacement set used by the
failed full image remains unvalidated and suspect, but this bisection does not
claim one IPA or RMNET module as the root cause because those modules were
replaced as a group and were not retested independently after the `wwan` fix.

Candidate A is the new controlled-v1 baseline: controlled WLAN, exact stock
cellular data plane, controlled system-DLKM, stock vendor-boot, and stock
VBMeta. Per the bisection plan, cellular migration stops here and must resume
only as a separate provider-batch experiment.

```text
PASS — CONTROLLED WLAN + STOCK CELLULAR BASELINE ESTABLISHED
```
